# iOS / macOS development

Modular rules live in `.claude/rules/` (path-scoped) and `.claude/always/` (session defaults). Path-scoped rules load when Claude reads matching files.

<!-- claude-ios-rules:start -->
@.claude/always/anti-overengineering.md
@.claude/always/agent-honesty.md
@.claude/always/agent-efficiency.md
<!-- claude-ios-rules:end -->
